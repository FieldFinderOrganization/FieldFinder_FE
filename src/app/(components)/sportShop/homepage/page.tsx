import InforBar from "@/utils/infoBar";
import TopBar from "@/utils/topBar";
import React from "react";

const page = () => {
  return (
    <div>
      <InforBar />
    </div>
  );
};

export default page;
